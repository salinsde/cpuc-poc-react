import { Button } from '@/components/styled';

const About = () => {
    return (
        <div className="App">
            <h1>About</h1>
            <Button variant="contained">MUI Button</Button>
        </div>
    );
};

export default About;
